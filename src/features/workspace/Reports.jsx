import { useState } from 'react'
import { Link } from 'react-router-dom'
import { useWorkspace } from './useWorkspace'
import { reportRows, reportCsv, download } from './reporting'
import { Heading, Empty } from './ui'
import { dateLabel } from './format'
export default function Reports() {
  const { actor, db } = useWorkspace()
  const [filters, setFilters] = useState({ learnerId: '', groupId: '', status: '', from: '', to: '' })
  const update = (key, value) => setFilters(old => ({ ...old, [key]: value }))
  const invalidDates = Boolean(filters.from && filters.to && filters.from > filters.to)
  const rows = reportRows(db, actor.id, filters)
  const submitted = rows.reduce((sum, r) => sum + r.quizSubmitted, 0)
  const stamp = new Date().toISOString().slice(0,10)
  return <><Heading title="Tiến độ & báo cáo" description="Theo dõi từng học sinh, xem lịch sử và xuất dữ liệu trong phạm vi tài khoản của bạn."/><div className="ws-toolbar ws-report-filters"><label>Học sinh<select value={filters.learnerId} onChange={e => update('learnerId', e.target.value)}><option value="">Tất cả học sinh</option>{db.learners.filter(l => l.ownerId === actor.id).map(l => <option key={l.id} value={l.id}>{l.name} · {l.code}</option>)}</select></label>{actor.role === 'teacher' && <label>Nhóm hiện tại<select value={filters.groupId} onChange={e => update('groupId', e.target.value)}><option value="">Tất cả nhóm</option>{db.groups.filter(g => g.ownerId === actor.id).map(g => <option key={g.id} value={g.id}>{g.name}</option>)}</select></label>}<label>Trạng thái<select value={filters.status} onChange={e => update('status', e.target.value)}><option value="">Tất cả trạng thái</option><option value="active">Hoạt động</option><option value="revoked">Đã thu hồi</option></select></label><label>Ngày nộp từ<input type="date" value={filters.from} onChange={e => update('from', e.target.value)}/></label><label>Đến ngày<input type="date" value={filters.to} min={filters.from} onChange={e => update('to', e.target.value)}/></label></div>{invalidDates && <p role="alert">Ngày kết thúc phải bằng hoặc sau ngày bắt đầu.</p>}<div className="ws-actions ws-export"><button disabled={invalidDates || !rows.length} className="ws-btn" onClick={() => download(reportCsv(rows), `finteen-report-${stamp}.csv`, 'text/csv;charset=utf-8')}>Xuất CSV (Excel)</button><button disabled={invalidDates || !rows.length} className="ws-btn" onClick={() => download(JSON.stringify({ generatedAt: new Date().toISOString(), filters, learners: rows }, null, 2), `finteen-report-${stamp}.json`, 'application/json')}>Xuất JSON chi tiết</button><button disabled={invalidDates || !rows.length} className="ws-btn" onClick={() => window.print()}>In / Lưu PDF</button></div><section className="ws-card ws-print-report"><h2>Báo cáo học tập · {actor.name}</h2><p>{rows.length} tài khoản · {submitted} bài đã nộp · Ngày nộp: {filters.from || 'Tất cả'} → {filters.to || 'Hiện tại'}</p><p className="ws-muted">Dữ liệu trò chơi chưa kết nối. Các chỉ số dưới đây chỉ tính bài kiểm tra Quiz đã nộp; tài khoản đã thu hồi vẫn giữ kết quả.</p>{rows.length ? <div className="ws-table-wrap"><table><thead><tr><th>Học sinh</th><th>Trạng thái</th><th>Quiz đã nộp</th><th>Trung bình</th><th>Chi tiết</th></tr></thead><tbody>{rows.map(r => <tr key={r.id}><td><strong>{r.name}</strong><small>{r.code}</small></td><td>{r.status === 'active' ? 'Hoạt động' : 'Đã thu hồi'}</td><td>{r.quizSubmitted}</td><td>{r.averagePercent === null ? 'Chưa có' : `${r.averagePercent}%`}</td><td><Link className="ws-text-link" to={`/dashboard/learners/${r.id}`}>Xem hành trình</Link>{r.results.map((s, i) => <small key={i}>{s.title}: {s.correct}/{s.total} · {dateLabel(s.submittedAt)}</small>)}</td></tr>)}</tbody></table></div> : <Empty>Không có học sinh phù hợp bộ lọc.</Empty>}</section></>
}
